<!DOCTYPE html>
<html lang="en">

<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="locale" content="en_US">
    <link rel="icon" href="https://static-exp1.licdn.com/scds/common/u/images/logos/favicons/v1/favicon.ico">
    <title>{{person.name}} | {{person.occupation}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./assets/clean.css">
</head>

<body style="margin-top: 0;">
    <main class="main" role="main">
        <section class="core-rail">

            {%- comment -%} TITLE CARD {%- endcomment -%}
            <section class="top-card-layout">
                <figure class="cover-img"></figure>
                <div class="top-card-layout__card"><img
                        class="entity-image entity-image--profile entity-image--circle-8 top-card-layout__entity-image"
                        alt="{{person.name}}" src="{{person.image_url}}">
                    <div class="top-card-layout__entity-info-container">
                        <div class="top-card-layout__entity-info">
                            <h1 class="top-card-layout__title">{{person.name}}</h1>
                            <h2 class="top-card-layout__headline">{{person.occupation}}, <span
                                    class="highlight">{{person.relationship_state}}</span></h2>
                            <h3 class="top-card-layout__first-subline"><span
                                    class="top-card__subline-item">{{person.location}}</span></h3>
                        </div>
                    </div>
                </div>
            </section>

            {%- comment -%} SUMMARY{%- endcomment -%}
            {% if person.summary %}
            <section class="summary pp-section" data-section="summary">
                <header class="summary__header pp-section__header">
                    <h2 class="summary__heading pp-section__heading">Summary</h2>
                </header>
                <p class="text-for-glossary-links">{{person.summary | newline_to_br}}</p>
            </section>
            {% endif %}

            {%- comment -%} POSITION SECTIONS {%- endcomment -%}
            {% for entry in sections %}
                {% include 'entry.html' %}
            {% endfor %}

            {%- comment -%} LANGUAGES {%- endcomment -%}
            {% if languages != empty %}
            <section class="pp-section">
                <header class="pp-section__header">
                    <h2 class="pp-section__heading">Languages</h2>
                </header>
                <ul class="languages__list">
                    {% for item in languages %}
                    <li class="result-card">
                        <div class="result-card__contents">
                            <h3 class="result-card__title">{{item.name}}</h3>
                            <h4 class="result-card__subtitle">{{item.level}}</h4>
                        </div>
                    </li>
                    {% endfor %}
                </ul>
            </section>
            {% endif %}

            {%- comment -%} GLOSSARY {%- endcomment -%}
            {% if glossary != empty %}
            <div class="top-card-layout glossary-card">
                <section class="pp-section">
                    <header class="pp-section__header">
                        <h2 class="pp-section__heading">Glossary</h2>
                    </header>
                    <ul>
                        {% for item in glossary %}
                        <li class="result-card">
                            <div class="result-card__contents">
                                <h3 class="result-card__title glossary">
                                    <a name="{{item.anchor}}">{{item.name}}</a>
                                </h3>
                                <h4 class="result-card__subtitle text-for-glossary-links" data-item="{{item.name}}">
                                    {{item.description | escape}}</h4>
                                {% if item.url %}
                                <div class="result-card__meta">
                                    <a href="{{item.url}}" target="_blank">{{item.url}}</a>
                                </div>
                                {% endif %}
                            </div>
                        </li>
                        {% endfor %}
                    </ul>
                </section>
            </div>
            {% endif %}
        </section>
    </main>
    <script>
        function runsOnPhone() {
            var check = false;
            (function(a){if(/(android|bb\d+|meego).+mobile|avantgo|bada\/|blackberry|blazer|compal|elaine|fennec|hiptop|iemobile|ip(hone|od)|iris|kindle|lge |maemo|midp|mmp|mobile.+firefox|netfront|opera m(ob|in)i|palm( os)?|phone|p(ixi|re)\/|plucker|pocket|psp|series(4|6)0|symbian|treo|up\.(browser|link)|vodafone|wap|windows ce|xda|xiino/i.test(a)||/1207|6310|6590|3gso|4thp|50[1-6]i|770s|802s|a wa|abac|ac(er|oo|s\-)|ai(ko|rn)|al(av|ca|co)|amoi|an(ex|ny|yw)|aptu|ar(ch|go)|as(te|us)|attw|au(di|\-m|r |s )|avan|be(ck|ll|nq)|bi(lb|rd)|bl(ac|az)|br(e|v)w|bumb|bw\-(n|u)|c55\/|capi|ccwa|cdm\-|cell|chtm|cldc|cmd\-|co(mp|nd)|craw|da(it|ll|ng)|dbte|dc\-s|devi|dica|dmob|do(c|p)o|ds(12|\-d)|el(49|ai)|em(l2|ul)|er(ic|k0)|esl8|ez([4-7]0|os|wa|ze)|fetc|fly(\-|_)|g1 u|g560|gene|gf\-5|g\-mo|go(\.w|od)|gr(ad|un)|haie|hcit|hd\-(m|p|t)|hei\-|hi(pt|ta)|hp( i|ip)|hs\-c|ht(c(\-| |_|a|g|p|s|t)|tp)|hu(aw|tc)|i\-(20|go|ma)|i230|iac( |\-|\/)|ibro|idea|ig01|ikom|im1k|inno|ipaq|iris|ja(t|v)a|jbro|jemu|jigs|kddi|keji|kgt( |\/)|klon|kpt |kwc\-|kyo(c|k)|le(no|xi)|lg( g|\/(k|l|u)|50|54|\-[a-w])|libw|lynx|m1\-w|m3ga|m50\/|ma(te|ui|xo)|mc(01|21|ca)|m\-cr|me(rc|ri)|mi(o8|oa|ts)|mmef|mo(01|02|bi|de|do|t(\-| |o|v)|zz)|mt(50|p1|v )|mwbp|mywa|n10[0-2]|n20[2-3]|n30(0|2)|n50(0|2|5)|n7(0(0|1)|10)|ne((c|m)\-|on|tf|wf|wg|wt)|nok(6|i)|nzph|o2im|op(ti|wv)|oran|owg1|p800|pan(a|d|t)|pdxg|pg(13|\-([1-8]|c))|phil|pire|pl(ay|uc)|pn\-2|po(ck|rt|se)|prox|psio|pt\-g|qa\-a|qc(07|12|21|32|60|\-[2-7]|i\-)|qtek|r380|r600|raks|rim9|ro(ve|zo)|s55\/|sa(ge|ma|mm|ms|ny|va)|sc(01|h\-|oo|p\-)|sdk\/|se(c(\-|0|1)|47|mc|nd|ri)|sgh\-|shar|sie(\-|m)|sk\-0|sl(45|id)|sm(al|ar|b3|it|t5)|so(ft|ny)|sp(01|h\-|v\-|v )|sy(01|mb)|t2(18|50)|t6(00|10|18)|ta(gt|lk)|tcl\-|tdg\-|tel(i|m)|tim\-|t\-mo|to(pl|sh)|ts(70|m\-|m3|m5)|tx\-9|up(\.b|g1|si)|utst|v400|v750|veri|vi(rg|te)|vk(40|5[0-3]|\-v)|vm40|voda|vulc|vx(52|53|60|61|70|80|81|83|85|98)|w3c(\-| )|webc|whit|wi(g |nc|nw)|wmlb|wonu|x700|yas\-|your|zeto|zte\-/i.test(a.substr(0,4))) check = true;})(navigator.userAgent||navigator.vendor||window.opera);
            return check;
        };
        var isRunningOnPhone = runsOnPhone();
        var glossary = [
            {% for item in glossary -%}
            {name: '{{item.name}}', re: new RegExp('{{item.match}}', 'ig'), anchor: '{{item.anchor}}', tooltip: decodeURIComponent('{{item.tooltip | escape | url_encode}}')},
            {% endfor %}
        ];
        var textContainers = document.querySelectorAll('.text-for-glossary-links')

        textContainers.forEach((node) => {
            var childNodes = [...node.childNodes];
            var itemOfNode = node.getAttribute('data-item')
            var newHtml = node.innerHTML;

            glossary.forEach(({
                name,
                re,
                anchor,
                tooltip
            }) => {
                if (itemOfNode != null && itemOfNode == name) {
                    return
                }
                for (let childNodeIndex = 0; childNodeIndex < childNodes.length; childNodeIndex++) {
                    const element = childNodes[childNodeIndex];
                    if (element.nodeType != document.TEXT_NODE) {
                        continue;
                    }
                    var text = element.textContent;
                    var matches = [...text.matchAll(re)];
                    if (matches.length == 0) {
                        continue;
                    }


                    var currentTextStart = 0;
                    var childNodesReplacingText = [];
                    for (let matchIndex = 0; matchIndex < 1; matchIndex++) {
                        const match = matches[matchIndex];

                        var textBeforeMatch = text.slice(currentTextStart, match.index);
                        currentTextStart = match.index + match[0].length;
                        childNodesReplacingText.push(document.createTextNode(textBeforeMatch));
                        var container = document.createElement('a');
                        container.setAttribute('class', 'glossary');
                        container.setAttribute('href', '#' + anchor);
                        var innerHTML = '';
                        if (!isRunningOnPhone) {
                            innerHTML = '<span class="tooltip">' + tooltip + '</span>';
                        }
                        innerHTML += match[0];
                        container.innerHTML = innerHTML;
                        childNodesReplacingText.push(container);
                    }
                    if (currentTextStart < text.length) {
                        childNodesReplacingText.push(document.createTextNode(text.slice(
                            currentTextStart)))
                    }
                    childNodes.splice(childNodeIndex, 1, ...childNodesReplacingText);
                    childNodeIndex += childNodesReplacingText.length;
                }
            })

            node.innerHTML = '';
            childNodes.forEach((child) => node.appendChild(child));
        })
    </script>
</body>

</html>